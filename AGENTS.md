# cmdutils — Agent Instructions

Persistent directives for AI assistants working on this project.

## Performance Standard

Before implementing any feature, **research** whether a better-performing or
more sophisticated alternative exists. Always use the most performant and
most sophisticated method available — never settle for naive or default approaches.

## Quality

- All code must compile without errors (`cargo build`).
- All existing tests must pass (`cargo test`).
- Output messages should be clear and informative.

*This file is checked at the start of every session. Update it as standards evolve.*
